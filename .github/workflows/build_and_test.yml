name: Build and Test

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v3
      - run: ./build_and_test
